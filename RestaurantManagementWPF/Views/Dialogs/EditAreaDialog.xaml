<Window x:Class="RestaurantManagementWPF.Views.Dialogs.EditAreaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Area" 
        Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Edit Area" 
                       FontSize="24" 
                       FontWeight="Bold"
                       Foreground="#333333"/>
            <TextBlock Text="Modify area information" 
                       FontSize="13"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1">
            
            <!-- Area Name -->
            <TextBlock Text="Area Name *" 
                       FontSize="14" 
                       FontWeight="SemiBold"
                       Foreground="#333333"
                       Margin="0,0,0,8"/>
            <TextBox Text="{Binding AreaName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBox}"
                     FontSize="14"
                     Margin="0,0,0,20"
                     x:Name="AreaNameTextBox"/>

            <!-- Area Status -->
            <TextBlock Text="Area Status" 
                       FontSize="14" 
                       FontWeight="SemiBold"
                       Foreground="#333333"
                       Margin="0,0,0,8"/>
            <ComboBox SelectedValue="{Binding AreaStatus}"
                      SelectedValuePath="Content"
                      FontSize="14"
                      Padding="10,8">
                <ComboBoxItem Content="Using"/>
                <ComboBoxItem Content="Maintenance"/>
            </ComboBox>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="SAVE"
                    Command="{Binding OKCommand}"
                    Click="OKButton_Click"
                    Style="{StaticResource PrimaryButton}"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Height="45"/>

            <Button Grid.Column="2"
                    Content="CANCEL"
                    Command="{Binding CancelCommand}"
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    FontSize="14"
                    Height="45"/>
        </Grid>
    </Grid>
</Window>
