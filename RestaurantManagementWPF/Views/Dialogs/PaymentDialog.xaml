<Window x:Class="RestaurantManagementWPF.Views.Dialogs.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Payment" 
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Payment" 
                       FontSize="24" 
                       FontWeight="Bold"
                       Foreground="#333333"/>
            <TextBlock Text="Complete the order and process payment" 
                       FontSize="13"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Order Summary -->
        <Border Grid.Row="1"
                Background="#F5F5F5"
                Padding="15"
                CornerRadius="8"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Table Info -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="ORDER SUMMARY" 
                               FontSize="12" 
                               FontWeight="Bold"
                               Foreground="#666666"
                               Margin="0,0,0,10"/>
                    <TextBlock FontSize="14" Foreground="#333333">
                        <Run Text="Table: " FontWeight="Normal"/>
                        <Run Text="{Binding TableName, Mode=OneWay}" FontWeight="SemiBold"/>
                    </TextBlock>
                    <TextBlock FontSize="14" Foreground="#333333" Margin="0,3,0,0">
                        <Run Text="Area: " FontWeight="Normal"/>
                        <Run Text="{Binding AreaName, Mode=OneWay}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>

                <Separator Grid.Row="1" Background="#E0E0E0" Margin="0,10"/>

                <!-- Items Count -->
                <TextBlock Grid.Row="2" FontSize="14" Foreground="#333333">
                    <Run Text="Total Items: " FontWeight="Normal"/>
                    <Run Text="{Binding ItemCount, Mode=OneWay}" FontWeight="SemiBold"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Order Items List -->
        <Border Grid.Row="2"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding OrderItems}" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#F0F0F0"
                                    BorderThickness="0,0,0,1"
                                    Padding="10,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding DishName}" 
                                                  FontSize="14"
                                                  FontWeight="SemiBold"
                                                  Foreground="#333333"/>
                                        <TextBlock FontSize="12" Foreground="#666666" Margin="0,2,0,0">
                                            <Run Text="{Binding Quantity}"/>
                                            <Run Text=" x "/>
                                            <Run Text="{Binding UnitPrice, StringFormat={}{0:N0}}"/>
                                            <Run Text=" VND"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1"
                                              Text="{Binding Subtotal, StringFormat={}{0:N0}}"
                                              FontSize="14"
                                              FontWeight="SemiBold"
                                              Foreground="{StaticResource PrimaryBrush}"
                                              VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Total Amount -->
        <Border Grid.Row="3"
                Background="#E8F4FF"
                Padding="15"
                CornerRadius="8"
                Margin="0,15,0,0">
            <Grid>
                <TextBlock Text="TOTAL AMOUNT:"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#333333"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0} VND}"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="COMPLETE PAYMENT"
                    Command="{Binding OKCommand}"
                    Style="{StaticResource PrimaryButton}"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Height="50"/>

            <Button Grid.Column="2"
                    Content="CANCEL"
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    FontSize="14"
                    Height="50"/>
        </Grid>
    </Grid>
</Window>
