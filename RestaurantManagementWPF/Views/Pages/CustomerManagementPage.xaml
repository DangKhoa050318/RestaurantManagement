<Page x:Class="RestaurantManagementWPF.Views.Pages.CustomerManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:RestaurantManagementWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Customer Management"
      Background="White">

    <Page.DataContext>
        <vm:CustomerManagementViewModel/>
    </Page.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="999">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading customers..." 
                           Foreground="#666666" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="4"
                             Foreground="{StaticResource PrimaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- Page Title -->
        <TextBlock Grid.Row="0" 
                   Text="Customer Management" 
                   FontSize="28" 
                   FontWeight="Bold"
                   Foreground="#333333"
                   Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search Box -->
            <TextBox Grid.Row="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBox}"
                     FontSize="14"
                     Width="400"
                     HorizontalAlignment="Left"
                     Padding="10,8">
                <TextBox.Resources>
                    <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="?? Search by name or phone..." 
                                                          Foreground="#999999" 
                                                          Margin="10,0,0,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Button Content="+ Add Customer"
                        Command="{Binding AddCustomerCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="15,8"
                        Margin="0,0,10,0"/>
                
                <Button Content="Edit"
                        Command="{Binding EditCustomerCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="15,8"
                        Margin="0,0,10,0"/>
                
                <Button Content="Delete"
                        Command="{Binding DeleteCustomerCommand}"
                        Style="{StaticResource DangerButton}"
                        Padding="15,8"
                        Margin="0,0,10,0"/>
                
                <Button Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Padding="15,8"/>
            </StackPanel>
        </Grid>

        <!-- Data Grid -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8">
            <DataGrid ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedCustomer}"
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      RowHeight="50">
                <DataGrid.Columns>
                    <!-- ID Column -->
                    <DataGridTextColumn Header="ID" 
                                       Binding="{Binding CustomerId}"
                                       Width="80"
                                       IsReadOnly="True"/>

                    <!-- Name Column -->
                    <DataGridTextColumn Header="Customer Name" 
                                       Binding="{Binding Fullname}"
                                       Width="2*"
                                       IsReadOnly="True"/>

                    <!-- Phone Column -->
                    <DataGridTextColumn Header="Phone Number" 
                                       Binding="{Binding Phone}"
                                       Width="*"
                                       IsReadOnly="True"/>

                    <!-- Order Count Column -->
                    <DataGridTemplateColumn Header="Total Orders" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Orders.Count, StringFormat={}{0} orders}"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource PrimaryBrush}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
